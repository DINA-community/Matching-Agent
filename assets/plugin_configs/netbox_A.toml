# Netbox plugin configuration

[DataSource]
# Plugin name must match the module name
plugin_name = "matcher_netbox_plugin"
timeout_seconds = 30
update_interval = 60

[DataSource.Netbox]
# Netbox-specific configuration
api_url = "https://netbox.example.com/api"
api_token = "abcdef123456"
