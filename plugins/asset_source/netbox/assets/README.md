# This directory contains the assets for the netbox plugin.

## netbox API

The api file is downloaded from the netbox API.
When updating the api file, please make sure to:

Delete all entries for

- devicetype_count
- device_count
- virtualmachine_count

from the file.